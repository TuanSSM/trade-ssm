# NATS server configuration for trade-ssm
# Credentials are injected via environment variables in docker-compose.

listen: 0.0.0.0:4222

authorization {
  users = [
    { user: $NATS_USER, password: $NATS_PASS }
  ]
}
